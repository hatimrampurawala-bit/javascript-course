"use strict"; // treat all JS code as newer version

// alert(3 + 3) // we are using nodeJS and not the browser so the syntax will be different

// Datetypes

let name = "Hatim"
let age = 25
let isLoggedIn = false

// number => 2 to the power of 53
// bigint => used for very big numbers generally in stock market sites or reddit or google
// string => " " OR ' '
// boolean => true OR false
// null => standalone value --Empty value
// undefined => Datetype not defined
// symbol => unique

// Object

console.log(typeof "Hatim");
console.log(typeof age);
